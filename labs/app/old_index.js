const serverHandle = function(req,res){

}
const http = require('http')
const handles =require('./old_handles')
http.createServer(handles.serverHandle).listen(8080)
// how to print something in javascript
